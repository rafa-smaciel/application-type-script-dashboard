import styled from 'styled-components' //34. Creating the Pie Chart Look

export const Container = styled.div`
    width: 48%auto;
    height: 260px;

    margin: 10px 0;

    background-color: ${props => props.theme.colors.tertiary};
    color: ${props => props.theme.colors.white};

    border-radius: 7px;

    display: flex;
`; //34. Creating the Pie Chart Look
export const SideLeft = styled.div`` //34. Creating the Pie Chart Look
export const LegendContainer = styled.div`` //34. Creating the Pie Chart Look
export const Legend = styled.div`` //34. Creating the Pie Chart Look
export const SideRight = styled.div`` //34. Creating the Pie Chart Look

